/**
 * These backend endpoints don't exist yet (blocked on filesystem write
 * access to the backend repo). Flip each flag to `true` once its endpoint
 * is implemented and deployed — that's the only change needed on the
 * frontend side to re-enable the real request.
 */
export const NOTIFICATIONS_API_ENABLED = false;
export const CALENDAR_API_ENABLED = false;
export const INFORMATION_API_ENABLED = false;

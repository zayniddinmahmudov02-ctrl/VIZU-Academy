/**
 * These backend endpoints don't exist yet. Flip each flag to `true` once
 * its endpoint is implemented and deployed — that's the only change
 * needed on the frontend side to re-enable the real request.
 */
export const NOTIFICATIONS_API_ENABLED = true;
export const CALENDAR_API_ENABLED = false;
export const INFORMATION_API_ENABLED = false;
